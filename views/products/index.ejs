<!doctype html>
<!--[if lt IE 7]>      <html class="no-js lt-ie9 lt-ie8 lt-ie7" lang=""> <![endif]-->
<!--[if IE 7]>         <html class="no-js lt-ie9 lt-ie8" lang=""> <![endif]-->
<!--[if IE 8]>         <html class="no-js lt-ie9" lang=""> <![endif]-->
<!--[if gt IE 8]><!-->
<html class="no-js" lang="">
<!--<![endif]-->

<head>
  <%include ../partials/head.ejs%>
    <style>
      .modal {
        display: block !important;
        z-index: 9999 !important;
        backdrop-filter: blur(14px) grayscale(0.6) opacity(12);
      }
     
    </style>
</head>

<body  ng-cloak ng-app="manageProducts" ng-controller="manageProductsController" ng-init="init()">
  <!-- Left Panel -->
  <%include ../partials/sidemenu.ejs%>
    <!-- /#left-panel -->
    <!-- Right Panel -->
    <div id="right-panel" class="right-panel">
      <!-- Header-->
      <%include ../partials/header.ejs%>
        <!-- /#header -->
        <!-- Content -->
        <div class="content">
          <div class="animated fadeIn">
            <div class="row">
              <div class="col-12">
                <div class="card">
                  <div class="card-header">
                    <strong class="card-title">Basic Table</strong>
                  </div>
                  <div class="card-body">
                    <div class="row">
                      <div class="col text-right">
                        <button class="btn btn-success" ng-click="openModal('create')"><i class="fa fa-plus"></i>
                          Add</button>
                      </div>
                    </div>
                    <div class="table-responsive mt-4">
                      <table class="table table-bordered table-striped text-center text-nowrap">
                        <thead class="bg-dark text-white">
                          <tr>
                            <th scope="col">#</th>
                            <th scope="col">Name</th>
                            <th scope="col">Price</th>
                            <th scope="col">Quantity</th>
                            <th scope="col">User Name</th>
                            <th scope="col">Action</th>
                          </tr>
                        </thead>
                        <tbody>
                          <tr ng-repeat="product in products track by $index">
                            <th>{{$index + 1}}</th>
                            <td>{{product.name}}</td>
                            <td>{{product.price}}</td>
                            <td>{{product.quantity}}</td>
                            <td>{{product.fullName}}</td>
                            <td>
                              <button class="btn btn-warning text-white" ng-click="openModal('edit', product, $index)"><i class="fa fa-edit"></i></button>
                              <button class="btn btn-danger text-white" ng-click="deleteProduct(product._id, $index)"><i class="fa fa-trash"></i></button>
                            </td>
                          </tr>
                        </tbody>
                      </table>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
          <!-- /.content -->
          <div class="clearfix"></div>
          <!-- Footer -->
          <%include ../partials/footer.ejs%>
            <!-- /.site-footer -->
        </div>
        <!-- /#right-panel -->
        <%include ./modal.ejs%>
          <%include ../partials/bodyScripts.ejs%>
            <script src="/assets/ngapps/manageProducts.js"></script>

</body>

</html>